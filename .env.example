# Portable fallback. macOS uses a saved Keychain entry first by default.
# Use --credential-source plain to explicitly bypass Keychain.
WFIRMA_LOGIN=you@example.com
WFIRMA_PASSWORD=replace-locally
# Optional: pin the accounting company returned by session_status.
# This is not a contractor/customer ID.
WFIRMA_COMPANY_ID=
